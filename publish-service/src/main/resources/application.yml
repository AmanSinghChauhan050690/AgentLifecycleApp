server:
  port: 8082

spring:
  application:
    name: publish-service

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    
# Local fallback for Feign when Eureka is not available during local testing
feign:
  client:
    config:
      agent-service:
        url: http://localhost:8081
